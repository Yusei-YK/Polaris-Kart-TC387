# VOFA 标定操作（当前固件）

## 1. 连接设置

- 波特率：`115200`
- 协议：`JustFloat`
- 当前固定输出 16 个通道；这不是串口文本 CSV。
- 每 20 ms 输出一帧（50 Hz）。

| 通道 | 含义 | 当前用途 |
|---|---|---|
| ch0 | 目标速度（脉冲/5 ms） | 速度 PID |
| ch1 | 滤波后平均速度（脉冲/5 ms） | 速度 PID |
| ch2 | 速度 PID 输出 duty | 速度 PID，数值较大，建议单独画图 |
| ch3 | IMU 航向角 yaw（度） | 检查惯导方向 |
| ch4 | MCU 收到的串口字节累计数 | 检查命令是否真正发进 MCU |
| ch5 | 转向绝对编码器原始值（0～4095） | 手掰转向标定 |
| ch6 | 转向相对临时中位的偏差 | 中位未标定前只参考变化趋势 |
| ch7 | 左后轮本拍脉冲（脉冲/5 ms） | 检查左右方向与速度 |
| ch8 | 右后轮本拍脉冲（脉冲/5 ms） | 检查左右方向与速度 |
| ch9 | 左后轮累计脉冲 | 推车距离标定 |
| ch10 | 右后轮累计脉冲 | 推车距离标定 |
| ch11 | 当前累计路程（m） | 左右编码器分别标定后得到的车体中心行程 |
| ch12 | 左后轮滤波速度（脉冲/5 ms） | 左轮独立 PI 反馈 |
| ch13 | 右后轮滤波速度（脉冲/5 ms） | 右轮独立 PI 反馈 |
| ch14 | 左后轮 PI 输出 duty | 检查左右驱动力补偿 |
| ch15 | 右后轮 PI 输出 duty | 检查左右驱动力补偿 |

## 2. 当前命令

- `e0`：关闭速度环，电机输出归零。
- `e1`：开启速度环。
- `t3`：目标速度设为 3 脉冲/5 ms。
- `p100`、`i0`、`d0`：修改速度 PID 参数。
- `z`：仅在 `e0` 状态下，清零 ch9、ch10 和 ch11。

每条命令后按回车。发命令时 ch4 应增加。

## 3. 第一轮必须采的数据

### A. 转向传感器（不让转向电机工作）

1. 上电后发 `e0`。
2. 用手把转向放在机械中间，记录 ch5。
3. 缓慢掰到机械最左，记录 ch5。
4. 缓慢掰到机械最右，记录 ch5。
5. 若经过 4095 后跳到 0 属于绝对编码器正常环绕；若随机乱跳才是接线或传感器问题。

### B. 编码器距离

1. 整车落地，车头摆正，发 `e0`，再发 `z`。
2. 开始保存 VOFA 数据，沿直线缓慢向前推 1 m（场地允许时推 3 m 更准）。
3. 停车后记录 ch9 和 ch10 的最终值并停止保存。
4. 重复三次，每次开始前重新发 `z`。
5. 正常情况：向前推时 ch9、ch10 同号且都为正，大小接近。若一正一负，先改编码器方向，不能继续算距离。

脉冲当量计算：

`每脉冲米数 = 实际距离 / ((左累计脉冲 + 右累计脉冲) / 2)`

完成后把三组 ch9/ch10、转向中/左/右三个 ch5 数值和 VOFA 截图发回，再进入速度 PI 和转向 PID。

## 4. 看波形时不要把所有通道叠在一起

- 编码器检查：只显示 ch7、ch8。
- 距离标定：只显示 ch9、ch10。
- 速度 PI：只显示 ch0、ch1；ch2 放到另一张图。
- 转向标定：只显示 ch5。
- 惯导检查：只显示 ch3。
