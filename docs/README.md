# 文档入口

当前只看两份：

1. `00_TC387_Minimal_Migration_Principles.md`：最高优先级原则。
2. `03_Kart_Migration_Plan.md`：执行计划。

其他文档是阅读记录和参考资料，不作为当前主线维护。

当前工程口径：直接在 `6.6_V1_TC264` 里写，后续按 TC387 硬件编译/适配；迁移时照搬 TopSpeed，少改，只改硬件差异。
