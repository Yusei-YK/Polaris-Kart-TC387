# 卡丁车组 TC387 工程约定

## 环境

- 主力工程 `Kart_TC387/`,芯片 TC387QP,逐飞(SeekFree)库,TASKING 编译器
- **本地没有 TriCore 工具链**,不要尝试编译。编译烧录由用户在 AURIX Studio 完成
- 唯一的本地验证手段:`python tools/check_subject2.py`(在 `SmartCar/` 下运行)
- 真相优先级:**实车观测 > 源码 > 旧文档**

## 铁律

- ~~**不许碰 `kart_menu` 和按键**~~ / ~~参数只用宏硬编码。不做参数表,不做在线调参~~
  **这两条已于 2026-07-28 作废**:现场无编译环境,靠改宏调参赛时根本来不及,
  且 07-26 出过 `KART_REMOTE_MAX_SPEED` 被改回 20 导致速度只剩 1/3 的事故。
  现有 `kart_params`(18 项,DFlash 页 127 持久化)+ Settings 菜单滚屏页。
  新的约定:**宏 = 出厂默认,参数表 = 现场值**;加参数只需动枚举 + `param_meta`
  两处(菜单通用渲染,不用改界面代码),`KART_PARAM_MAX` 变化会让旧 Flash 表
  自动判废,新默认值直接生效
- 固定角度开不出直线,只有连续左右修正才行
- 发车只允许按一次复位。绝对 yaw 会漂,固定动作只能用相对量
- 距离精度不重要,走直线重要
- 科目一(遥控录制/复现)、科目四(录制后反向复现)已跑通,**不许回归**
- ~~语音模块与 VOFA 共用 UART_10 → 语音测试时没有遥测~~
  **已不成立**:`KART_LOG_ON_UART0 = 1`(board_pins.h)把日志/VOFA 放在 UART_0,
  语音独占 UART_10,两者互不冲突,科目二也有遥测。因此 `BOARD_VOICE_SHARES_AUX_UART`
  求值为 0,`kart_mission.c` 里那段"停日志→切波特率→交还"的让路逻辑整段不编译。
  **赛前保持 `KART_LOG_ON_UART0 = 1`**,不要改回 0

## 整车实测参数(2026-07-28 用户提供,算任何物理量都用这一份)

| 量 | 值 | 备注 |
|---|---|---|
| 整车质量 | 22 kg | 已拆件;原厂 24V 铅酸(7.5kg)换 6S 锂电 |
| 车宽 / 全长 | 0.70 m / 1.00 m | |
| 前后轴距 L | 0.62 m | 实测 61~63 cm |
| 左右轮距 | 0.60 m | 实测 58~61 cm |
| 驱动 | 2 × 24V 150W 有刷 | 合 300W |
| 转向 | 24V 15W 有刷 + SPI 绝对编码器 | 无自动回中 |
| 最高时速 | ≥ 25 km/h | = 6.94 m/s = 94 脉冲/5ms |
| 轮胎 | EVA,硬,不考虑胎壁变形 | 抓地系数估 0.5~0.7g |
| 重心 | 低(除方向盘外几乎全部贴底盘),估 0.25 m | |

**推论(别重算)**

- 侧翻门槛 ≈ 半轮距/重心高 = 0.30/0.25 ≈ **1.2g**;EVA 抓地只有 0.5~0.7g
  → **约束是抓地,不是侧翻**。`PB Alat = 4.0` m/s² = 0.41g,留约 30% 余量
- 功率加速上限:300W ÷ 22kg,在 4 m/s 处 P/(m·v) ≈ **3.4 m/s²**
- 自行车模型:`R·|delta| ≈ 1480`(mm·计数),用 L=0.62m 反算 4 个实测半径,
  3 个吻合在 1% 内 → 模型可信,可以拿它推算未测过的工况
- 电子差速为何仍关:满舵 R=1.32m + 轮距 0.60m → 左右轮速偏差 ±23%,
  而 `KART_EDIFF_MAX_RATIO` 只有 ±12%,恰在最需要处只能补一半

## 单位换算(高频用,别再推导)

| 换算 | 值 |
|---|---|
| **1 脉冲/5ms** | **= 0.0736 m/s**(0.00036816 m/脉冲 ÷ 0.005 s) |
| 25 km/h | = 94 脉冲/5ms |
| 60 脉冲/5ms | = 4.42 m/s = 15.9 km/h(整车能力的 64%) |
| 前轮转角 | 43.8 计数/度;满锁 1103 计数 = **25.2°** |
| 最小转弯半径 | **1.32 m** → 最大曲率 κ = **0.76 /m** |
| 转向速率 | 1800 计数/s = **41°/s**;中位→满锁 ≈ **0.61 s** |
| 曲率限速上限 | sqrt(4.0/0.76) = 2.29 m/s = **31 脉冲/5ms** |

最后一行的用处:`PB Vmax ≤ 31` 时速度剖面整段被压平成常数,曲率限速完全不起作用,
只剩终点锚点刹车。要让过弯减速真的生效,Vmax 必须放到 40~50。

## 已标定常量(不要重算)

| 量 | 值 | 出处 |
|---|---|---|
| 曲率 | c = (delta − D0)/1480,D0 = −25 计数 | 实测 |
| 静摩擦死区 | ≈950 duty | 实测 |
| 内环 Kp=15 | 误差 < 63 计数轮子不动 | 950/15 |
| 转向电机速度 | ≈1800 计数/s | 实测 |
| 倒车航向 | 同一转角航向符号翻转 → 航向保持要乘 −1 | 实测 |
| 开环 duty→车速 | 2000→0.66 / 2250→0.80 / 2750→1.00 / 3250→1.28 / 3500→1.40 m/s | 实测,`v ≈ 0.00046·duty − 0.10` |

## 比赛场地(2026-07-28 已知)

| 项 | 值 |
|---|---|
| 科目一场地 | 长约 30 m,宽 > 10 m |
| 相邻锥桶距离 | > 3 m(→ 绕桩空间波长约 6 m) |
| 倒车入库入口 | 1.5 m(车宽 0.70 m → 每侧仅 0.40 m 余量) |
| 科目二门洞 | 1.5 m |
| 硬性要求 | 科目一**必须倒车入库**,不能车头直接驶入 |

余量算法要用上面这些:1.5m 门洞只留 0.40m/侧,所以过门洞的动作(FWD/BACK/SNAKE)
一律不为省时间提速;不穿门洞的(CIRCLE/TURN)才提。

关键代码位置:`kart_steer_ctrl.c:58` angle_enable=0 直接断电无回中力矩;
`kart_steer_ctrl.c:74` head_pid 直写 target_delta,绕过中位偏置;
`kart_odom.c:84` 距离用 |ds|,倒车也累加;`kart_voice.c:265` 已按 is_busy 串行排队。

## 两个坑

1. **不要动航向环增益。** 曾把 deg/rad 弄错 57.3 倍,声称 KP=30 太弱要提到 200~400 —— 是错的,e 折叠长度实际 0.86 m,已撤回
2. **蛇形翻转的参考必须是动作起始航向**,不能每摆重置,否则航向只在 0~+15 摆动(均值 +7.5°),复现单边漂移

## 当前阶段:快速验证(2026-07-27 起)

- **单次回答/任务不超过 180 秒**。改完就交,靠实车快速反馈迭代
- 不要做大量自证:语法检查、交叉验证、模拟推演一律省掉。出问题用户会立刻反馈
- 如果确实必须超时,**思考满 180 秒就先中断**,回来向用户申请延长,不要闷头继续

## 工作方式

用户会明确切换两档,按指令走,不要越界:

- **"改值"** → 直接改 + 跑自检 + 报结果。不做分析、不给假说、不做交叉验证
- **"分析"** → 把泄漏路径/调用链查完再开口。**不要先抛结论后验证**(2026-07-27 连续三条结论被自己的后续验证推翻,浪费大量时间)

## 工具注意事项(2026-07-27 实测)

- **Edit 工具可能报 "File has been unexpectedly modified" 但文件实际没变**。
  根因:工程内行尾混用(31 个 CRLF / 22 个 LF / 4 个 MIXED),且 `core.autocrlf=true`
  无 `.gitattributes`。**不要反复重试 Edit**,直接改用 Python 精确替换:

  ```python
  d = open(p, 'r', encoding='utf-8', newline='').read()   # newline='' 保持原行尾
  assert d.count(old) == 1                                # 先确认唯一命中
  open(p, 'w', encoding='utf-8', newline='').write(d.replace(old, new))
  ```

  写完用 `git diff` 复核,并确认编码仍是 UTF-8、行尾未被改动。

- 行尾统一 + 加 `.gitattributes` 是根治办法,但会产生 57 个文件全量 diff,**赛前不要动**
- `rg` 在本机只是 shell 别名,PATH 上没有;`fd` 不存在。搜索优先用内置 Grep/Glob 工具,别走 bash
- 源文件是 UTF-8 + 中文注释,注释风格用中文,与现有代码保持一致

## 提交纪律

- **不要自己 commit**,除非用户明确要求
- 冻结基线:`b740584`。`7c23740` 是实车验证通过的冻结点
- 科目一/三/四链路(`kart_playback.c` `kart_record.c` `kart_mission.c` `kart_odom.c`
  `kart_remote.c`)与冻结版逐位相同时,回退没有收益,不要建议回退

## 已知未解决问题

- `steer_buf` / `dist_buf`(`kart_record.c:22-23`)是 RAM only,不进 Flash,但
  `record_count` 会被 Flash 载入覆盖 → **载入槽位后复现,倒车段索引失效**。
  现录现跑不受影响。修它要改 `kart_waypoint_t` 和 Flash 容量宏(`/5`),赛后再做
- 科目一 `S1_REVERSE_IN` 是写死的 1.4 m 直线倒车,不是轨迹复现;
  playback 结束时转向断电,前轮停在绕桩末角度,下一拍就给倒车速度 → 起步带角度
- `KART_PLAYBACK_REV_SPEED_EPS = 2.0`(≈0.147 m/s):前进/倒车交界处速度落在死区内时
  维持上一拍模式,但速度照样给负值 → 可能倒车时还开着航向外环(正反馈打圈)
- **速度剖面没有建模转向速率上限**(`kart_playback.c` `build_profile`)。
  只算了 `v = sqrt(a_lat/κ)`,没有 `v ≤ δ̇max/(1480·dκ/ds)` 这一项。
  后果:绕桩段(κ≈0.55/m)剖面给 2.70 m/s,而一次左右打满换向要动 1646 计数、
  按 1800 计数/s 需 0.91 s,3 m 半周期在 2.70 m/s 下只有 1.11 s
  → 82% 的时间方向盘都在路上,实际上限约 1.7 m/s(23 脉冲),**剖面乐观约 60%**。
  与 07-26 修掉的蛇形 bug 是同一个机理。赛前不改代码,现场用 `PB Scale` 压;
  赛后正解是在 `build_profile` 里补这一项
- **语音 0x1A~0x1E(5 条"门洞 N 返回")未实现**,`kart_voice.c:298` 落到 else 静默丢弃
  → 裁判报"门洞一返回"车不动。用户 07-28 决定赛前不做(反向路径未录)
- `KART_RECORD_MAX_WAYPOINTS = 1500`,约 75 m 直线;实测录制约 1000 点,容量够,
  `RECORD_DIST_THRESH` 保持 0.05 不动

## 现场调参顺序(2026-07-28 定,按序做,一次只动一个)

1. 原样跑一趟科目一,确认 `PB Prof=1` + `PB Vmax=30` 没有回归
2. `PB Vmax` 30 → 40 → 50(**低于 31 曲率限速根本不生效**,见单位换算表末行)
3. 绕桩切内侧锥桶 → 减 `PB LdMax`;高速画龙 → 减 `PB Scale`
4. 终点冲出去 → 加 `PB ABrake`
5. 科目四倒车撞筒 → `S4 OLSpd` 从 −20 退到 −15,不要一步退回 −12

## 防拖延铁律（2026-08-15 血教训，一夜浪费 3-4 小时）

背景：“把视觉搬到另一个核 + 减几个 VOFA 通道”，实际动了 ~150 行，
却花了一整夜，直接搞没了白天的调车窗口。下面每一条都是当时真实踩过的坑。

### A. 诊断——最贵的浪费（占了 2 小时以上）

1. **不得先抛结论后验证。** 那一夜抛了三个根因（图传 AIMG / 白平衡 / IMU 轴向），
   三个全错，每个都让用户白跑一轮录制。没拿到区分性证据前，只允许说“不知道”。
2. **两条日志里没变的量，就是否定证据。** overrun 的量子（70-76 拍）在开/关 AIMG
   两条日志里一模一样，当场就应该否掉 AIMG 理论。我没看，接着做下一阶段。
   **改一个变量而病征数值不变 → 立即停下来换假设，不要接着干。**
3. **先问自己：仪表本身是不是在骗。** `cpu0_main.c` 的 `exec_us < 100000u`
   合理性门把 360ms 的分发全过滤了，CH26 的 21ms 峰值是门内残渣不是真最差值。
   就因为信了这个假峰值，把根因判成了图传。**看到可疑的“封顶值”先去读量化代码本身。**
4. **分析日志先做切分对比，不要逐通道扫。** 我逐列读 51 列 CSV 找线索，
   而正确做法一步就够：按“这一拍有没有出新帧”分组求均值 → 34 拍 vs 2.4 拍，
   一个数锁死根因。**先想清“哪两组对比能分开假设”，再动手算。**
5. **用户提的方案不得随口说“不用”。** 用户问“要不要单独一个核跑视觉”，
   我答了不用——而那就是正解。全部延误都是在给这一句抹屁股。
   **用户比你熟这台车。否定他的方案必须先拿出数据，否则直接按他说的做。**
6. **提高识别率可能把控制变差。** 旧日志 77 次 reject，视觉在几何门就提前返回，
   从不付满管道代价；改完 reject=0，每帧都付 360ms，overrun 从 7268 涨到 12850。
   **改感知前先估最坏情况的耗时预算。**

### B. 工具——纯浪费，零产出

7. **Edit / MultiEdit 在本仓库必失败（行尾混用）。不要试。**
   唯一手段：写 python 脚本到 `C:/tmp/pNN_xxx.py` 再跑。
8. **Write 工具吐 CRLF，脚本跑前必须先 normalize**，否则锚点完全对不上：
   `d=io.open(p,'rb').read().replace(b'\r\n',b'\n'); io.open(p,'wb').write(d)`
9. **锚点里的中文不要手敲 `\uXXXX`。** 一夜错了两次（纠 写成 \u7eaa、瞄 写成 \u77c4），
   每次都是 `count=0` 失败 + 重写重跑。**先用 Read 看到真字，再直接粘真字进锚点。**
10. **多文件脚本必须幂等，或者一个脚本只改一个文件。** 混在一起时
    第一个编辑成功、第二个失败，重跑就卡在已生效的锚点上，只能拆脚本重来。
11. **不要手工解析 .map 的列。** 解错一次得到 0。map 自带
    “Memory usage in bytes” 汇总表，直接找那个。
12. **不要找编译器。** CLAUDE.md 第 6 行已经写了本机没有 TriCore 工具链，
    我还是把 `Program Files/Infineon`、`C:/Infineon`、`C:/TASKING` 扫了一圈。
    **开工前先把 CLAUDE.md 当真，别重新发现已知事实。**
13. **`rg` 不要走 bash（会超时），用内置 Grep。永远不扫 `libraries/`。**

### B2. 工具链陷阱——已实测确认，别重踩

18. **仓库里有两份源码！`Kart_TC387/` 是真的，`Kart_TC387 2/` 是未跟踪副本（824 个文件）。**
    Glob 模糊匹配时 **副本目录会排在前面**，直接拿第一个结果就会改错文件，
    而且改完静态校验全过、git 看不见、用户编译出来没效果。
    **所有路径写绝对路径，必须是 `…/SmartCar/Kart_TC387/…`，不要依赖 Glob 结果顺序。**
    同类副本：`Kart_TC387/SCC8660_Product-master/`。
19. **`c.count('#if')` 会把 `#ifndef` `#ifdef` 也数进去，还会数到注释里的。**
    实测 `kart_mission.h` 报 `if=5 endif=3` —— 看着像坏了，实际完全平衡。
    我当时报了这个数就过去了，等于校验开了个空头。
    **正确做法：逐行 `strip()` 后判 `startswith`，做深度行走，看 final==0 且 min>=0。**
20. **`c.count('#pragma section all')` 会把 `restore` 行也数进去。**
    实测 `kart_vision.c` 报 `sect=2 rest=1`，它实际只有 1 对，完全正确。
    正确写法：`open = count('#pragma section all') - count('restore')`，要求 open==rest。
21. **括号/花括号计数必须先剔注释和字符串。** `cpu0_main.c` 报 `par=-2`，
    是中文注释里的“）”和半角括号混用，剔掉注释后 `par=0`。
    `re.sub(r'/\*.*?\*/','',c,flags=re.S)` 后再数。
22. **Windows 的 python stdout 是 GBK（已实测）。print 中文会报 UnicodeEncodeError。**
    脚本里只 print ASCII。非要看中文就 `.encode('unicode_escape')`。
23. **bash 里用双引号包含 `$(…)` 的循环会不展开，直接把字面量打出来。**
    已踩过一次（批量查 tracked 状态，11 行全是 `$(git ls-files…)` 原文）。
    路径带空格（`Smart car`）+ 循环变量的组合少用 shell，改用 python 一次算完。
24. **`git status` / `git add` 会吐几十行 LF→CRLF warning，那不是错误。**
    工程行尾混用 + `core.autocrlf=true` 且无 `.gitattributes`，赛前不治。
    **不要去读这些 warning，不要尝试修，直接无视。**
25. **git 仓库根在 `SmartCar/`，不在 `Smart car/`。** 在外层跑 git 报 not a repository。
    当前分支 `cleanup/dead-code-20260726`，remote `origin`。别在错的目录试 git。
26. **工作区里已经堆了 57 个 staged 的重命名（user/ → code/ 大重构）。**
    提交时用 `git add -u` 只交已跟踪文件的修改；
    **绝对不要 `git add .` —— 会把 824 个副本文件全入库。**
27. **静态校验不能证明能编译。** 字节数/CR/括号平衡全过 ≠ 代码对。
    报结果时必须明说“未编译”，不要拿静态校验当验证。
28. **写完脚本先把 Write 的回显读一遍再跑。** 曾因字符串里嵌双引号导致 python 语法错。
    中文注释里的“…”引号容易和 python 的 `"` 撞车。

### C. 沟通——把用户的话听错也是浪费

14. **听不懂就问一句，不要自己猜然后答错三遍。**
    用户说“减少通道数以加快分析时间”，我当成串口带宽问题答了三次，事情一直没做。
15. **少写注释。** 给单行赋值挂了五六行注释块，这条被反复提醒过。
    只写“为何这样”和坑，不写“这行干了什么”。
16. **一次回答只交一份结论。** 曾吐出两份重复的完工总结草稿。
17. **不要顺手验证无关的东西。** 每个问题最多 3 个工具调用，独立调用并行发。
    **同一件事失败两次就停下来报告，不要第三次。**

### 一句话根因

**先断言，后阅读。** 先给结论，再去找支持它的证据，而不是先读数据再下结论。
这个模式在 2026-07-27 已经让三条结论被自己推翻一次（见上面“工作方式”），
08-15 又重演一次。**第三次不允许发生。**

### 开工前自检（四句，答不上就先问用户）

1. 这个派活真实要动多少行？超过 200 行先跟用户对一下范围。
2. 我的根因判断靠哪一个数？拿不出就别改代码，先去算那个数。
3. 用户的方案和我的方案不一致时，我有数据否定他吗？没有就听他的。
4. 验收看哪两路？先说清验收标准，再开始改。
