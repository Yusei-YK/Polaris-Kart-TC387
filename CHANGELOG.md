# 更新记录

按阶段记，不按语义化版本——这个仓库只有一份真正发布过的固件，就是实车跑完赛
那一份。想复现赛场上的车，检出标签 `race-final-2026`，别用 `main`，也别用当前
分支尖端。

## 现在的状态（2026-09-07）

- **实车完赛的固件是标签 `race-final-2026`**（2026-08-24，第二十一届智能车竞赛，
  全国二等奖）。赛场上跑的就是这一份。
- 赛后的活都在 `feature/pedal-ecu` 上，从 `24f4c66` 分出，比 `main` 多 20 条；
  `main` 又比远端 `mine/main` 多 18 条。也就是说远端还落后当前尖端 38 条，
  没有推过。
- 赛后唯一的功能改动是 CH32 踏板盒接入速度环（人工驾驶模式 `MISSION_PEDAL`），
  其余全是注释订正和文档。
- **赛后这些改动一次都没编译过**——本机没有 AURIX 工具链，只做了源码级核对。
  上板前必须先在 AURIX Studio 里编一遍。

## 未发布

### 新增

- CH32 踏板盒接入速度环，新模式 `MISSION_PEDAL`：油门、刹车、R 挡由踏板盒经
  串口给，遥控留着总闸随时能抢回来。提速上限、R 挡、全力刹车、运行页同步
  都在里面。`fd05e58` `b0220d3`

### 修复

- 目标机编译的四处阻塞与告警。`ca05e22`
- 开机页副标题居中错位。`6e5f271`
- `user/isr.c` 里 1073 个 U+FFFD 乱码字符复原，85 行受损，全是逐飞的原注释。
  抬头取自 `user/isr.h`（同一段样板且干净），正文取自 GBK 祖先 `055f328`，
  按 ASCII 骨架单调对齐。判乱码要数 `EF BF BD` 字节——U+FFFD 本身就是合法的
  UTF-8 序列，靠“能不能解码”查不出来。`252688c`

### 删除

- 旧主循环回退分支和三个点阵屏自检死循环，`cpu0_main.c` 当时 577 → 475 行。
  调度器 overrun 恒 0，回退没有留的理由。`478526e`
- 死宏 `FOLLOW_ENABLE`：全仓库零读取点，跟随一直是无条件调的活代码。`579fc83`
- 没人读的 `BOOT_DISPLAY_FRAMES`。`fc17c15`
- 三条 AI 署名行，只留成稿日期。`3a15bd6`

### 文档与注释

- 94 个文件按 include 层级过了一遍注释，订正与代码事实矛盾的说法。
  改法是【保留原来错的说法、在旁边注明它错在哪】，不默默换掉，因为仓库要交给
  下一届。行号引用一律换成函数名、宏名、槽名这类不随行号漂的锚点。
  `88e7ab9` 之后共 54 条提交，其中 47 条是纯注释。
- 仓库收拾：分支收成一条 `main` 加五个标签、工程从 `Kart_TC387/` 上提到根、
  移出厂商库副本、README 重写并分开写参考来源与自研部分、开发日志和技术报告
  入库、捞回灯板逻辑主机测试（唯一一份能脱离硬件跑的测试）。
  `5793a19` `c3b76ea` `c754680` `9470c27`
- 函数名对照表列进 `docs/TODO.md` 的 D5：六条词不达意、五条命名不统一、
  七条核实过没问题。只出判断没改名，改名要连调用点一起动。`4953df3`

## race-final-2026 —— 2026-08-24

实车完赛版本，全国二等奖。

- 科目三倒车段改用跟随时录下的速度，油门扳机发车。`195ca7e`

## flash-s3-20260823 —— 2026-08-23

烧进板子的快照，留着对照。`93bceda`

## 2026-08-15 ~ 08-17（无标签）

- 视觉搬到 core3，跟随环不再被 360ms 的识别卡死；日志通道 51 减到 26。`e4edd84`
- 视觉快照的超龄门跟着搬核一起放大，否则每帧都掉 LOST。`b00f3cb`
- 跟随、视觉、人车联动入库。`5f19dde`

## 2026-07-28 ~ 08-04（无标签）

- 科目四倒车免按键；换新按键板，修好 LEFT/MID/START 共脚；菜单里能在线调速。
  `da67645`
- 换齿轮、编码器、转向电机之后重标转向；削减停录到倒车之间的真空期。`dd96d93`

## freeze-20260726-light-voice —— 2026-07-26

赛前冻结点：灯板软扫加语音驱动图案，实车验证通过，箭头方向已修正。`7c23740`

## snapshot-pre-multicore-20260722 —— 2026-07-22

多核重构前的代码快照，原 `main` 分支尖端。`c818976`

## 2026-06-04 ~ 07-06（起步）

- 纳管 TC387 主力工程源码与库依赖，移出历史工程，整合文档。`055f328` `f3acbb4`
- 所有硬件引脚收进 `board_pins.h`。`f2e6c89`
- 从逐飞 TC264 模板加 IMU 代码起步。`f184db1`

## 还有一个标签

`archive/teammate-main-20260722` 是队友那条线的最后快照。那几条分叉提交里的
灯板和 TLD7002 驱动 `main` 里都有、而且更新，只是目录重排过；真正只在那条线上的
是 8 个文件，处置写在 `docs/TODO.md` 的 C6 和 C7。
