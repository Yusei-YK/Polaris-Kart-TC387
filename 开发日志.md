# 此文件为卡丁快跑 Kart_TC387 的开发日志

按时间正序。硬件硬结论统一见 `docs/03_硬件排查与第二版PCB.md`。

标记：【已解决】【硬件·待验】【安全隐患·待查】【待查】【待补】

## 第一版

_26.6.29_{
研读 TopSpeed 极速越野工程，定下迁移思路：保留分层架构、控制节拍、PID、统一 Power 输出和 Mission/Subject 状态机，只换硬件底层；
梳理主板 v1 外设分配，规划 v2 引脚：无线串口跑实时状态和日志，新增 SPI 预留给无线模块和科目三通信；
}

_26.6.30_{
电机驱动和左后编码器跑通；
右后编码器、转向编码器读数不对，开始排查；
}

_26.7.1_{
复查代码，软件链路没有明显断点，问题在硬件；
右后编码器是机械故障：亚克力安装板断裂导致编码器错位。换新的，接线供电焊接还得继续排；
}

_26.7.2_{
【已解决】转向绝对编码器不通是引脚问题：MOSI 落在 `P23.1`，而它不是 TC387 SPI4 的合法 MOSI 脚。第一版飞线，第二版定版前对调 MOSI/CS 焊盘；
顺手整理仓库。真正在开发的 `Kart_TC387/` 一直没完整纳管，被跟踪的反倒是早期工程的编译产物和 IDE 缓存，旧 README 还停在 TC264/GNSS 阶段。补 `.gitignore`、清索引、开始写这份日志；
}

_26.7.3_{
新编码器快递没到，硬件推不动，休整一天；
}

_26.7.4_{
按键方案定稿：主板原有按键删掉，改成板间接口 + 独立按键板，承载旋转编码器、五向按键、START 和电源指示灯；
硬件卡着，先推不依赖硬件的算法层。确认 TopSpeed 的舵机控制搬不过来——卡丁是直流转向电机加前轮绝对编码器，角度环得自己写；
}

_26.7.5_{
算法层一天铺完：几何运算、Madgwick 6DOF 解算、位置式 PID、后轮平均速度单环，主循环和 5ms 中断都接上了；
调试台从 CSV 换成 VOFA+ JustFloat，加了在线改 PID 和目标速度的命令，省得反复烧写；
第一次架空调速度环。默认 Kp 太小推不动空载，加到几百才转，纯 P 的稳态误差也很明显；
【硬件·待验】后轮速度是左右均值，怎么加都卡在 19~22，像是左后驱动烧了被平均值盖住。得拆出左右独立通道才能确认；
【安全隐患·待查】发 `e0` 关速度环电机没停，最后靠 reset。按仲裁逻辑该交给遥控轮询清零 duty，根因没定位，通电前必须查清；
}

_26.7.6_{
【已解决】转向 raw 不刷新，是 `kart_steer_abs_update()` 只在 init 调了一次没进主循环。补上，VOFA 加了转向 raw 和中位偏差两个通道，为角度环备反馈量；
【已解决】翻完 TopSpeed 的转向闭环，确认只能抄 PID 算子和控制结构。它用舵机、PWM 直接给角度，没有前轮角度传感器，那个 `angle_ctrler` 控的是车身航向而不是前轮；
【硬件事故】DIR/PWM 接反已经改过了，但转向打死的过程中还是烧穿了单驱芯片。断电后电流从单驱倒灌回主板，把电源二极管和 MOS 管也带走了；
当前几道闸口：`e0` 停不住没定位前不再大电流调速度环；左后驱动疑似烧毁，不能再用后轮均值判断双轮；转向硬件修好前不做闭环；右后编码器还要用 GPIO 电平区分是硬件问题还是定时器配置问题；
把历史工程移出仓库归档，根 README 改成当前仓库入口；
}

_26.7.13_{
新建 `examples/`，以子模块引入逐飞 TLD7002 点阵例程，锁定提交、保留 GPL-3.0 和上游历史，来源和迁移边界写进文档；
核对官方 Demo 才发现和自己主板记录差得多：官方要 UART1、2Mbps，还要 GPIN0、SYNC 中断和 7 路行选，而 `board_pins.h` 只记了几个脚，GPIN0/SYNC 根本没映射。这次不动灯板引脚，等对着原理图确认完再说；
按科目细则整理出 8 种灯光任务，写 `kart_light`，输出与硬件无关的 7x15 点阵帧，动画全靠 `kart_light_update(elapsed_ms)` 推进，不用阻塞延时，双缓冲发布避免扫描中断读到半帧；
修了几个自己挖的坑：非法枚举没回退到关闭、亮度字段跨中断的可见性、大时间步下的相位溢出；
写了主机端测试逐行校验所有图案和边界，C99 严格编译通过。还没进 `cpu0_main.c`，也没上板；
复核细则：规则允许用不同图案表示，雨刷也可以用灯表示，不要求逐像素复刻，当前显示逻辑够用；
}

## 第二版

_26.7.15_{
回到编码器这层，重写 `kart_encoder` 的接口和标定口径，把左右轮的脉冲当量单独拉出来，不再共用一个系数；
}

_26.7.17_{
编码器实现落地，左右轮 delta 拆成独立通道送 VOFA，之前"均值盖住单侧故障"的坑现在能直接看出来了；
}

_26.7.18_{
灯板开始接线。TLD7002 要 6~10V 供电，实测 LM2596 输出只有 4.5V，电位器没调到位；
【硬件·待验】板子是学妹画的，问题不止供电这一处，接线一天下来没点亮。先把 `kart_light.h` 的接口按实际硬件对齐；
【待补】旧板具体还有哪些设计错误（走线、译码、接口针数），我得回忆一下补上；
}

_26.7.19_{
坑踩太多记不住了，停下来写文档。`docs/` 下六份 01~05 加 README 一天写完，把硬件排查、引脚、控制结构、例程来源这些都归位；
}

_26.7.21_{
屏幕和按键板做硬件自测，结论单独写了 `屏幕按键硬件自测总结`；
【硬件·待验】按键板五向按键焊接短路，多个方向联动。之前一直勉强用着，没腾出手来修；
【硬件·待验】并口 IPS200 屏幕物理开裂；
}

_26.7.22_{
灯板那块板不打算继续修了，自己重新设计重新打板；
喇叭 `kart_horn` 补上；
想把 IMU 和里程计挪到 CPU1、录制轮询挪到 CPU2，动手前先推了个快照存档（"保存多核重构前当前代码"）；
CPU1/CPU2 的入口和 `kart_multicore` 包装层都写好了，但 `KART_MULTICORE_COMPAT_ENABLE` 留 0 没启用——跨核 cache 一致性心里没底，单核算力目前也够；
【待补】当时是跑出问题才关的，还是纯粹不想冒风险，我再想想；
}

_26.7.24_{
里程计 `kart_odom` 和 IMU 这一层调通，CPU3 入口也铺好了；
}

_26.7.25_{
新打的灯板接线加排查前后弄了好几天，今天终于恢复正常，能稳定点亮了；
}

_26.7.26_{
灯板收工：软扫加语音驱动图案实车验证通过，推了个"冻结"提交锁住；
接着把历史遗留的错误注释和失效代码清了一遍，顺手修掉两个已经坏掉的灯板自检；
日志写到这里就断了，后面这十几天全是赛前和赛中，一直没顾上补；
}

## 省赛（长春）

_26.7.27_{
上午哈尔滨出发，下午到长春，安顿完就开始调车；
主要在跟加速较劲。起步顿挫查了半天，最后定在执行器饱和上，不是编码器标定的问题：目标一跳，Kp 直接要八千多 duty，功率同步那边每拍只放 400，PID 的抗饱和看不见这个限幅，积分一路攒满，冲过头之后反向刹车，异号 duty 又触发过零保护强制断电几十毫秒——顿挫就是这么来的。结论是要在目标端限速率，而不是在输出端补；
遥控这层也顺手改了；
}

_26.7.28_{
熬到凌晨三点推了一版：科目四倒车免按键、按键板重焊、菜单在线调速；
按键板终于重新焊了一块，五向和 START 的短路问题解决，功能都正常了。之前一直勉强用着，现场再凑不下去了；
速度斜坡从 1.5 改到 0.4。算了一下，1.5 相当于每秒 22 米的加速度，而车实际只有 3 米多，胎的抓地极限也就 5~7，旧值比车能做到的快了五到九倍，等于限幅根本没生效，只是把病遮住了；
菜单在线调速是被现场逼出来的——场地没有上位机，参数改不动就只能一遍遍烧写，太慢了；
上午补交，下午接着调；
}

_26.7.29_{
上午科目一，下午科目二；
【硬件事故】下午前轮齿轮掉了，在赛场修了一下午车，勉强完赛；
晚上回住处把前轮拆掉重修，加固完继续弄到很晚；
这天软件也改了一大堆：Flash 路径格式从 `KPTH` 升到 `KPT2`，每点 5 字扩到 7 字，头部加了起点位姿（yaw 和 x/y），以后复刻不用再手动把车摆回起点对朝向；参数一次加了 14 个，凑到 26 个全挂到菜单上，现场彻底不用烧写；复刻加了速度规划；科目二语音跑完自己回原位的 GOTO 写成了倒车、转向、直行、对轴四相状态机；
代价是 `KART_PARAM_MAX` 一变，昨天现场调好的整张参数表全废，之前录的路径也全读不出来了。赛前三天敢这么干，是觉得省下的烧写时间更值；
}

_26.7.30_{
上午科目三，比赛结束；
齿轮加固后重标：换齿轮和编码器之后原来的标定全作废，脉冲当量重新量了一遍（跟旧值差得不多），转向行程重标到 ±1064。转向电机没换，后轮减速比也没变；
改动面太大肉眼核不过来，写了 `tools/check_subject2.py` 做结构自检；
【待补】各科目跑成什么样、有没有罚分、最终名次；
}

## 赛后

_26.8.4_{
接着收科目四的尾。停录到倒车之间有一两秒真空期，拆开看是三段：滑停判据等得太死、停稳确认压了 500ms、交接时前进段攒的正积分还在反抗倒车目标；
判停阈值从 2.0 放到 4.0，确认时间 500ms 压到 150ms，交接前先失能清一次速度环积分。倒车目标本身就会刹住前进惯量，不用干等它滑到停；
副作用记一下：过了解锁里程之后，中途停顿超过 150ms 就会提前触发倒车；
另外查日志发现横向反馈的 Ke 一直是 0，也就是说 OLMode=1 的横向纠偏从来没真正开过，一路只有航向 P 在工作。绕桶看着没事是因为有余量，入库就撞了。按回归定出符号，Ke 应该给正值；
【待补】赛后这段是为决赛准备，还是单纯把没做完的补完；
}

